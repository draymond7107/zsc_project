http请求的流程
        0，域名解析
        1，客户端与服务端建立连接（三次握手）
        2，客户端发送数据到服务器
        3，服务器收到请求，进行处理，然后将处理结果相应给客户端
        4，关闭连接（四次挥手）

    域名解析
        域名解析：由域名解析出要访问的ip

    源IP与目标IP建立连接


    http三次握手
        1，Client请求连接试探到Server，携带连接报文（报文：请求行，清透头，请求体）
        2，Server监听到连接请求报文，同意连接，则向Client发送确认，TCP报文首部中的SYN 和 ACK都置1
        3，Client收到确认后还需再次发送确认，同时携带要发送给Server的数据。



    http四次挥手
        1，主机1（可以使客户端，也可以是服务器端）设置Sequence Number，向主机2发送一个FIN报文段；主机1进入FIN_WAIT_1状态，表明，主机1没有数据要发送到主机2
        2，主机2收到主机1的FIN报文段，向主机1回复ACK报文，主机1进入FIN_WAIT_2状态；主机2告诉主机1，我“同意”你的关闭请求
        3，主机2向主机1发送FIN报文段，请求关闭连接，同时主机2进入LAST_ACK状态；
        4，主机1收到主机2发送的FIN报文段，向主机2发送ACK报文段，然后主机1进入TIME_WAIT状态；主机2收到主机1的ACK报文段以后，就关闭连接；此时，主机1等待2MSL
        后依然没有收到回复，则证明Server端已正常关闭，那好，主机1也可以关闭连接了。

    错误信息
        200 OK 客户端请求成功
        400 Bad Request 客户端有请求语法错误，不能被服务端所理解
        401 Unsuthorized 请求没有经过授权，这个状态必须和www-Authenicate 报头域一起使用
        403 Forbidden 服务器收到请求，但是拒绝提供服务
        404 Not Found   请求的资源不存在，输入了错误的url
        500 Intenal Server Error 服务器发生不可预知的错误
        503 Sercer Unsavailable 服务器当前时间不能处理客户端的请求，过一段时间又可以恢复正常
